# ccm_lit

Hartree-Fock (HF), CCD and CCSD Python codebase.

This repository contains working code to perform HF, CCD, and CCSD calculations.

Usage

- Inspect `main.py` to run the calculation.

Requirements

- See `requirements.txt` for Python dependencies.

License

Add a license if desired.
